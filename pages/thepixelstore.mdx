![PixelPulse Store](https://cdn.jsdelivr.net/gh/iambhvsh/surecdn@main/images/app-store/appstorelogo.png)
  
  # ⚒️ Pixel Store
  
  Welcome to the **Pixel Store**, an innovative ReactJs and Framework7 based application marketplace. Our mission is to provide a seamless and visually captivating user experience, akin to the renowned iOS App Store.

  # ⬇️ Installation

  ```bash
  git clone https://github.com/iambhvsh/thepixelstore.git
  ```

  # 📦 Dependencies

  Don't install all dependencies; it will break the project. Instead, run this command to install only the necessary dependencies:

  ```bash
  npm install --save-dev webpack webpack-cli webpack-dev-server
  ```

  # 🧪 Test

  Run **thepixelstore** locally:

  ```bash
  npm start
  ```

  or

  ```bash
  npm run start
  ```

  # 🧰 Build for Production or Development

  ```bash
  npm run build-prod
  ```

  or

  ```bash
  npm run build-dev
  ```

  # 🔗 Links
  
  [PixelPulse Store V6](https://example.com/V5)  
  [PixelPulse Store V5](https://pixelpulsestore.vercel.app)
